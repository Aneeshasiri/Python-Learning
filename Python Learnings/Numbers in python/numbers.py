import math

pi = 3.14

x = 1
y = 2
z = 6

print(round(pi))# round function will round that number for us so pi rounded to nearst whole integer is 3 #
print(math.ceil(pi))# it round a number up to nearst whole integer so pi is 4 #
print(math.floor(pi))# pi rounded down would be 3#
print(abs(pi))# absoulte value function, it will give exact vale of the number#
print(pow(pi, 2))# power function, pow will raise a base number to power number#
print(math.sqrt(pi))# give the square root value of the given number#
print(max(x, y, z))# gives the max number of the given variables value#
print(min(x, y, z))# gives the lowest number of teh given variables value#
